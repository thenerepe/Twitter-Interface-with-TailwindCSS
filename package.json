{
  "devDependencies": {
    "tailwindcss": "^3.2.7"
  },
  "scripts": {
    "build": "npx tailwindcss -i tailwind.css -o src/style.css --minify",
    "watch": "npx tailwindcss -i tailwind.css -o src/style.css --watch --minify" 
  }
}
